apiVersion: v1
kind: Service
metadata:
  name: {{ include "cx-ve.fullname" . }}
  labels:
    {{- include "cx-ve.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "cx-ve.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
