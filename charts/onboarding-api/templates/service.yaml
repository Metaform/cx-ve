apiVersion: v1
kind: Service
metadata:
  name: {{ include "onboarding-api.fullname" . }}
  labels:
    {{- include "onboarding-api.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "onboarding-api.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
