apiVersion: v1
kind: Service
metadata:
  name: {{ include "verification-ui.fullname" . }}
  labels:
    {{- include "verification-ui.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "verification-ui.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
